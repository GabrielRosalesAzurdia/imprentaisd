/**
 * 	Responses given by the api, if the format of the responses change just
 *  change the values
 */
export enum Responses {
    SUCCESS_DELETE = "{success:204}",
    SUCCESS_PUT = "{success:200}",
    SUCCESS_GET = "{success:200}",
}
